
# This file marks app/routes as a Python package
# It can be empty or contain package-level code
# But should NOT import the blueprints to avoid duplicate registration

# You can add package-level variables or functions if needed
# For example:
API_VERSION = 'v1'